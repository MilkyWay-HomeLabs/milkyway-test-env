events {}

http {
  include       /etc/nginx/mime.types;
  default_type  application/octet-stream;
  sendfile      on;
  gzip          on;
  gzip_types    text/plain text/css application/javascript application/json image/svg+xml;

  server {
    listen 80;
    server_name _;

    location /robak/app/ {
      root /usr/share/nginx/html;
      index index.html;
      try_files $uri $uri/ /robak/app/index.html;
    }

    location /robak/app/assets/ {
      alias /usr/share/nginx/html/robak/app/assets/;
      expires 7d;
      access_log off;
      try_files $uri =404;
    }
  }
}
